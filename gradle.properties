org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=true

# NOTE: local proxy (127.0.0.1:7892) was used when it was running to reach
# maven.neoforged.net. It is currently DOWN and breaks resolution, so it is
# commented out. All NeoForge artifacts are already in the Gradle cache;
# Maven Central is directly reachable. If a fresh NeoForge artifact is ever
# needed, re-enable these four lines while the proxy is running.
#systemProp.https.proxyHost=127.0.0.1
#systemProp.https.proxyPort=7892
#systemProp.http.proxyHost=127.0.0.1
#systemProp.http.proxyPort=7892

group=net.example.jrtameall
version=1.3.0
